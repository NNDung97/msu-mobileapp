import 'package:flutter/material.dart';

class AppColors {
  // ===== BACKGROUND =====
  static const background = Color(0xFF0A0A0F); // Đen tím

  // ===== CARD =====
  static const card = Color(0xFF1A0F1F);        // Tím rất đậm
  static const cardGradient = Color(0xFF24122B); // Gradient tím nhẹ
  static const border = Color(0xFF3A2A40);       // Viền tím khói

  // ===== ICON =====
  static const iconPrimary = Color(0xFFFF5C65);  // Đỏ Kurumi
  static const iconSecondary = Color(0xFFB49BFF); // Tím lavender (phối với card)

  // ===== TEXT =====
  static const text = Color(0xFFFFF5E6);          // Kem ấm sang trọng
  static const textSecondary = Color(0xFFAAA6B3);  // Xám tím đẹp hơn A8A8A8

  // ===== BUTTON =====
  static const buttonPrimary = Color(0xFFFF3B47); // Đỏ Kurumi
  static const buttonPrimaryLight = Color(0xFFFF5C65); // Red gradient 2

  static const buttonGradientStart = Color(0xFFFF3B47);
  static const buttonGradientEnd = Color(0xFFFF5C65);

  // ===== BANNER PLACEHOLDER =====
  static const bannerPlaceholder = Color(0xFF1D1625);

  // ===== SHADOW =====
  static const shadow = Colors.black54;

  // ===== NAVBAR & APPBAR =====
  static const appBar = Color(0xFF131218);  // Đen tím
  static const navBar = Color(0xFF131218);

  static const unselected = Color(0xFF9A9A9A); // Xám nhạt
  static const selected = Color(0xFFFF5C65);   // Red Kurumi highlight
}
